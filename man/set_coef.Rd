% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simfun.R
\name{set_coef}
\alias{set_coef}
\title{Generate Diagonal and Interaction Coefficients}
\usage{
set_coef(s, alpha = par.control(), beta = par.control(), negative = TRUE)
}
\arguments{
\item{s}{Integer. The dimension of the coefficient matrix.}

\item{alpha}{A list generated by \code{\link[=par.control]{par.control()}} that specifies how
to generate the \eqn{s} diagonal coefficients. User-supplied values override
defaults through partial matching via \code{\link[utils:modifyList]{utils::modifyList()}}.}

\item{beta}{A list generated by \code{\link[=par.control]{par.control()}} that specifies how
to generate the \eqn{s \times s} matrix of off-diagonal coefficients.
User-supplied values override defaults through \code{\link[utils:modifyList]{utils::modifyList()}}.}

\item{negative}{Logical. If \code{TRUE} (default), all coefficients are multiplied
by \code{-1}, making them negative. If \code{FALSE}, coefficients remain positive.}
}
\value{
A numeric \eqn{s \times s} matrix of coefficients.
}
\description{
Creates an \eqn{s \times s} coefficient matrix whose diagonal elements
represent self-regulation terms and whose off-diagonal elements represent
interaction strengths. The distribution and parameters used to generate these
coefficients are controlled via \code{alpha} and \code{beta}, which are
lists produced by \code{\link[=par.control]{par.control()}}.
}
\details{
The diagonal elements are generated from \code{alpha}, while the full
matrix (before overwriting the diagonal) is generated from \code{beta}.
Each control list specifies the type of distribution (\code{"constant"}, \code{"exp"},
\code{"unif"}, or \code{"normal"}) and its associated parameters.

For \code{type = "constant"}, \code{mu} may be either a scalar or a vector of length
\eqn{s} for \code{alpha}, and either a scalar or an \eqn{s \times s}
matrix for \code{beta}. For all other types, random draws are generated
from the appropriate distribution.

The diagonal of the resulting matrix is always replaced by the values
generated from \code{alpha}, ensuring that diagonal and off-diagonal
coefficients can be specified independently. The \code{negative} argument allows
the user to control the sign of the coefficients.
}
\examples{
\dontrun{

# Default: negative constant self-effects and interaction effects
set_coef(s = 4)

# Uniform variation in diagonal coefficients
set_coef(s = 4, alpha = list(type = "unif", min = 0.5, max = 2))

# Normal variation in off-diagonal interaction strengths
set_coef(s = 4, beta = list(type = "normal", mu = 0, sd = 0.2))

# Keep coefficients positive
set_coef(s = 4, negative = FALSE)

}

}
