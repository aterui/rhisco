% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{lag_base}
\alias{lag_base}
\title{Generate Lagged or Lead Versions of a Vector}
\usage{
lag_base(x, k = 1L)
}
\arguments{
\item{x}{A numeric, character, or logical vector. The vector to be lagged or led.}

\item{k}{An integer scalar specifying the number of positions to shift.
\itemize{
\item A positive value of \code{k} produces a \strong{lag}, moving elements forward and filling the first \code{k} elements with \code{NA}.
\item A negative value of \code{k} produces a \strong{lead}, moving elements backward and filling the last \code{abs(k)} elements with \code{NA}.
}}
}
\value{
A vector of the same length as \code{x}, with elements shifted according to \code{k}.
}
\description{
This function creates a lagged (or lead) version of a numeric or character vector by shifting its elements
forward or backward by a specified number of positions.
Missing values (\code{NA}) are used to fill in positions that fall outside the original range after shifting.
}
\examples{
x <- 1:5
lag_base(x, k = 1)   # returns c(NA, 1, 2, 3, 4)
lag_base(x, k = -1)  # returns c(2, 3, 4, 5, NA)
lag_base(x, k = 0)   # returns c(1, 2, 3, 4, 5)

}
\author{
Akira Terui, \email{hanabi0111@gmail.com}
}
