% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{lag_block}
\alias{lag_block}
\title{Generate Lagged Values Within Groups}
\usage{
lag_block(data, y = "n", t = "t", index = NULL)
}
\arguments{
\item{data}{A data frame containing at least the grouping, time, and response variables.}

\item{y}{Character string specifying the name of the response variable to be lagged.
Default is \code{"n"}.}

\item{t}{Character string specifying the name of the time or sequence variable
used to order observations within each group. Default is \code{"t"}.}

\item{index}{Character string specifying the grouping variable (e.g., species, site, ID).
Default is \code{"species"}.}
}
\value{
A data frame identical to \code{data} but with an additional column named
\code{<y>_lag}, containing the one-step lagged values of the response variable
computed within each group.
}
\description{
This function computes lagged values of a specified variable within each group
defined by a grouping (index) column. It first orders data within each group by
a time or sequence variable, then applies a one-step lag to the target variable.
The resulting data frame includes a new column containing lagged values.
}
\examples{
data <- data.frame(
  species = rep(c("A", "B"), each = 4),
  t = rep(1:4, 2),
  n = c(5, 6, 7, 8, 2, 3, 4, 5)
)

lag_block(data)

}
\seealso{
\code{\link{lag_base}}, \code{\link{split}}
}
\author{
Akira Terui, \email{hanabi0111@gmail.com}
}
