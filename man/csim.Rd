% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simfun.R
\name{csim}
\alias{csim}
\title{Simulate Community Dynamics}
\usage{
csim(
  ts = 1000,
  warmup = 100,
  burnin = 100,
  s = 10,
  r = par.control(),
  alpha = par.control(),
  beta = par.control(),
  model = c("ricker", "bh"),
  negative = NULL,
  sd_eps = 0.1,
  stochastic = FALSE,
  pg = par.control(),
  extinct = 0,
  trim = TRUE
)
}
\arguments{
\item{ts}{Integer. Number of timesteps to retain after warmup and burn-in.}

\item{warmup}{Integer. Number of warmup steps with propagule introduction (excluded from output if \code{trim = TRUE}).}

\item{burnin}{Integer. Number of burn-in steps without propagule introduction (excluded from output if \code{trim = TRUE}).}

\item{s}{Integer. Number of species in the community.}

\item{r}{List or scalar passed to \code{par.control()}. Controls intrinsic growth rates.}

\item{alpha}{List or scalar passed to \code{par.control()}. Controls competition coefficients.}

\item{beta}{List or scalar passed to \code{par.control()}. Controls facilitation coefficients.}

\item{model}{Character. Population model: \code{"ricker"} or \code{"bh"}.}

\item{negative}{Logical. Whether interaction coefficients are set negative by default.
If \code{NULL}, defaults to \code{TRUE} for Ricker and \code{FALSE} for Beverton-Holt.}

\item{sd_eps}{Numeric. Standard deviation of environmental stochasticity.}

\item{stochastic}{Logical. If \code{TRUE}, applies environmental stochasticity to growth.}

\item{pg}{Object passed to \code{par.control()}. Defines the propagule introduction schedule.}

\item{extinct}{Numeric. Density threshold below which species density is set to zero.}

\item{trim}{Logical. If \code{TRUE} (default), returns only post warmup+burn-in timesteps.}
}
\value{
A tibble with columns:
\itemize{
\item \code{ts} timestep
\item \code{species} species ID
\item \code{density} species density
}

Returned object includes attributes:
\itemize{
\item \code{"r"} intrinsic growth parameters
\item \code{"coef"} species interaction matrix
}
}
\description{
Simulates multispecies community dynamics using either the Ricker or
Beverton-Holt population model, with species interactions and
environmental stochasticity. Species can be initialized and introduced
according to a user-specified propagule schedule during warmup.
}
\details{
The model runs for \code{warmup + burnin + ts} timesteps, and returns species
densities in a tidy data format. Warmup and burn-in periods can be excluded
from the output using \code{trim = TRUE}.
}
