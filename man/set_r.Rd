% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simfun.R
\name{set_r}
\alias{set_r}
\title{Generate Species Growth Rates}
\usage{
set_r(s, r = par.control())
}
\arguments{
\item{s}{Integer. The number of species, i.e., the length of the resulting
growth rate vector.}

\item{r}{A list generated by \code{\link[=par.control]{par.control()}} that specifies how to
generate the vector of growth rates. User-supplied values override defaults
through partial matching via \code{\link[utils:modifyList]{utils::modifyList()}}.}
}
\value{
A numeric vector of length \eqn{s} containing the growth rates.
}
\description{
Generates a numeric vector of intrinsic growth rates (\eqn{r}) for a set of
species. The length of the vector is specified by \code{s}, and the distribution
and parameters used to generate these rates are controlled via \code{r},
which is a list produced by \code{\link[=par.control]{par.control()}}.
}
\details{
The \code{r} list specifies the type of distribution (\code{"constant"},
\code{"exp"}, \code{"unif"}, or \code{"normal"}) and its associated parameters (\code{mu}, \code{sd},
\code{min}, \code{max}). For \code{dist = "constant"}, \code{mu} may be a scalar or a vector of
length \eqn{s}. For other types, random draws are generated from the specified
distribution. For \code{exp} type, \code{mu} must be positive.
}
\examples{
\dontrun{
# Default: constant growth rates
set_r(s = 5)

# Exponentially distributed growth rates
set_r(s = 5, r = list(dist = "exp", mu = 0.5))

# Uniformly distributed growth rates
set_r(s = 5, r = list(dist = "unif", min = 0, max = 1))

# Normally distributed growth rates
set_r(s = 5, r = list(dist = "normal", mu = 0.5, sd = 0.1))
}

}
